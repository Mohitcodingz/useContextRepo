Flow: - 
#navbar got the cart to display the result based on the data #ProductCard file sends but let's assume that because of the reasons such as props drilling. to avoid that we share the #ProductCard (sender)-> #CartContext.(middleman) this contains that function we run when we use it.
--> Navbar.jsx(Receiver)  